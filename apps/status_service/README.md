# status_service

Status and revocation service for W3C `credentialStatus` resources backed by
isomer state projected from KERI TEL information.

The current Python implementation lives in `src/vc_isomer/status.py` and
`src/vc_isomer/service.py`.


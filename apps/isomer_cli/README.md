# isomer_cli

CLI entrypoints for issuing VCDM 1.1 VC-JWT/VP-JWT artifacts, verifying
Isomer resources, and serving local status/verifier services.

The current Python implementation lives in `src/vc_isomer/cli/`.
